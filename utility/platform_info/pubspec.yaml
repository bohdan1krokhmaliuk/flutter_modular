name: platform_info
description: Platform information utility
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  analysis:
  flutter_test:
    sdk: flutter
